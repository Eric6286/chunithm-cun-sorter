# 更新记录

> 倒序，最新在上。

## 2026-08-28 — CI 从 v2.0 起一直是红的

五次 Build 全部失败，而本地 `pytest`、本地打包都一次复现不出来。两处毛病，
都只在 runner 上犯；而且第一处挡着第二处，修完才看见后面还有一个。

### 测试数的那个标记，也在 CI 的检出路径里

挂的是 `test_hooking_is_idempotent`：它数的是整个 start.bat 里 `chunithm-cun-sorter`
出现了几次，断言等于 1。GitHub Actions 把仓库检出在
`D:\a\chunithm-cun-sorter\chunithm-cun-sorter\`，于是注入行成了这样——

```
start "chunithm-cun-sorter" "…\pythonw.exe" "D:\a\chunithm-cun-sorter\chunithm-cun-sorter\main.py" --watch
```

窗口标题一个、路径两个，一共三个。开发机的路径里没有这个串，所以只有 CI 会红。

`hook()` 本身是幂等的（插之前先把所有带标记的行滤掉），没有动它。改的是测试：
改成数**注入行的条数**——行首为 `start "chunithm-cun-sorter"` 的那些——而不是数标记。
另加一条 `..._when_our_own_path_carries_the_marker`，把 `launch_command()` 换成 CI
那种带标记的路径，让这个场景在开发机上也跑得到。

### 打包脚本的第一句 print

`build.py` 满屏中文，而输出编码随所在机器：开发机是中文 Windows，GBK 编得出；
runner 的控制台是 cp1252，`print(f"{APP_NAME} {__version__}")` 当场
`UnicodeEncodeError`，四步一步没跑。脚本开头把 `stdout` / `stderr` reconfigure 成
UTF-8——英文 Windows 上手动打包会踩同一处，所以修在脚本里而不是只改 CI。
两个 workflow 另外设了 `PYTHONIOENCODING: utf-8`，兜住 pytest 这些别的工具。

`release.yml` 跑的是同一个 `build.py`，所以下次打 tag 发版本来也会挂在这里。

## 2026-08-28 — v2.0.3：改名为「寸录」

原名是「今天你寸了吗」，一整句话当程序名，窗口标题、开始菜单、任务栏上都嫌长。
新名字留着「寸」——那是这程序唯一在做的事——去掉句子。

真源仍是 `core/version.py` 的 `APP_NAME`，界面标题、托盘、exe 文件名、快捷方式、
自启注册表值全从它推。跟着改的还有 `APP_USER_MODEL_ID`（`JinTianNiCunLeMa.App`
→ `CunLu.App`）和 DGHub 插件名（「寸录 · 联动」）。

**没动的**：仓库名 `chunithm-cun-sorter`、`APP_SLUG`、安装包文件名、数据目录
`%LOCALAPPDATA%\ChunithmCunSorter\`、安装器 `AppId`、单实例互斥体名、start.bat
里那行的标记。所以升级是原地覆盖，配置和统计数据一条不丢。

### 两处以名字为键的旧记录要清

改名把两样东西留在了原地，都不清就是「看着正常，实际失效」：

- **开机自启**：注册表 Run 键的值名就是应用名，旧那条既不会被 `is_enabled()` 看见、
  命令里指的又是已经不存在的 `今天你寸了吗.exe`。`core/autostart.py` 加了
  `migrate_legacy()`，启动时搬一次：删旧值、按新名字重写命令。
- **旧 exe 和旧快捷方式**：`AppId` 没变所以是覆盖安装，但 Inno 不会删它不认识的文件。
  `installer.iss` 加了 `[InstallDelete]` 点名删掉 `今天你寸了吗.exe` 和开始菜单 /
  桌面上那两个 `.lnk`，否则看着像装了两个程序。

安装目录仍沿用旧名（Inno 记着上次装在哪），想改过来只能先卸载再装。

### 顺手

`installer.iss` 里有句注释写成了 `core` + 垂直制表符 + `ersion.py`——当初 `\v` 被当
转义吃掉了。补回反斜杠。

## 2026-08-28 — v2.0.2：Mica 真的铺上了，版面收进一列

### Mica 少了一步，等于没开

v2.0.1 只调了 `DwmSetWindowAttribute(DWMWA_SYSTEMBACKDROP_TYPE)`，回读也是对的
（`hr=S_OK, value=2`），但界面一点没变——**材质只铺在标题栏那一条，客户区还是老样子**。
少的是 `DwmExtendFrameIntoClientArea` 传四边 `-1`，把玻璃摊到整个客户区。补上之后，
窗口底色实测是 `(29,33,35)`，和它底下那块壁纸 `(133,153,166)` 一样是蓝>绿>红，
材质确实在取壁纸。

顺带把内容区那层半透明图层也去掉了，侧栏和内容区一律不画，材质露得更满，
只剩卡片浮在上面。

### 版面：内容收进一列并居中

窗口能拉到 2000 宽，而「标签在最左、控件在最右」的行一旦拉开，中间就是一大片空白，
眼睛要横扫整行才对得上。内容列封顶 840 居中，多出来的宽度留白——macOS 的「系统设置」
和 WinUI 的设置页都是这么做的。三页共用 `widgets.page_shell()`，滚动、留白、列宽一处定义。

### 其它

- 三页都补上了大标题（配置 / 统计 / 运行）。之前「配置」页直接从「目录」开始，像没做完。
- 只读的目录路径不再画成输入框——它不是给人填的，按行里的「值」右对齐排版就够了。
- 判定规则的「删除」改成一个淡叉，指上去才变红。一列五个红「删除」太吵。
- 卡片补一道 7% 白的细边；Mica 底下光靠底色差分不出层次。
- 分组之间的间距从 16 提到 24。

## 2026-08-28 — v2.0.1：界面字号按像素给，行不再被压塌

只动界面，判定 / OCR / 联动那几条路一行没碰。

### 字号：pt 写成了 px 该有的数

`ui/theme.py` 里那张 HIG 字体样式表（正文 13、小标题 11、脚注 10、大数字 22）是
**像素**值——macOS 上 1pt 就是 1px，照抄过来却用了 `setPointSize` 和 QSS 的 `pt`。
Qt 在 Windows 上按 96 DPI 换算，13pt 落到屏幕上是 17px，整屏字大三分之一。
全部改成 `setPixelSize` / `px`，和 ChuniOptionManager 那份对齐。

### 「运行」页的行叠在一起

字一大，页面就装不下；而 `Row` 用 `setMinimumHeight(ROW_HEIGHT)` 定行高，Qt 的
`qSmartMinSize` 里显式设过的最小高度会**顶掉**布局算出来的那个——于是行被压到比内容
还矮，标签和副标题直接画在同一片像素上。三处一起改：

- 行高的地板挪到标签上，行本身改成纵向 `Fixed`，布局压不动；
- 副标题换成不换行、放不下从中间省略的 `ElidedLabel`（路径那种长文本原先会悄悄折成
  两三行，把同一张卡片里别的行挤扁）；
- 「运行」页补上和「配置」页一样的滚动容器，窗口矮了就滚，而不是把卡片压扁。

### 副标题一直空着的那行

`Row` 只在构造时传了非空文案才建副标题部件，`Row("接入 start.bat", "")` 于是永远建不出来，
后面 `set_sublabel()` 全部打空——那行的 start.bat 路径从 v2.0 起就没显示过。
改成一律建好、按内容显示或隐藏。

### 背景换成 Mica

窗口底改成 Windows 11 的 Mica 材质：`DwmSetWindowAttribute` 的
`DWMWA_SYSTEMBACKDROP_TYPE = DWMSBT_MAINWINDOW`（21H2 退回未公开的 `DWMWA_MICA_EFFECT`）。
坑在于光调这个不够——材质是 DWM 铺在窗口**后面**的，窗口自己那层像素不透明就把它整个盖住了。
所以样式表多了一套底色：`QMainWindow` 和侧栏完全不画，内容区 / 卡片 / 输入框逐层压半透明
（55% / 55% / 60%），越靠上越不透。对话框保持不透明，材质是给主窗口那一层用的。

Windows 10 或者系统里关掉了「透明效果」时自动退回原来的不透明底色，界面照常。

### 其它

- **下拉框的箭头自己画了。** 样式表把 `QComboBox::drop-down` 的按钮框去掉之后，Windows
  样式连箭头一起不画了，下拉框看着和只读输入框一模一样。QSS 里拿边框拼三角在 Qt 里会画成
  一个实心方块，所以新增 `widgets.Combo`，在 `paintEvent` 里描一笔。应用内的下拉框都换成它。
- 侧栏 176 → 152，导航项行高钉成 30——不给行高 Qt 会按带图标那一档算，选中的橙色块高得像块砖。
- 「统计」页顶部那行「暂无数据 · 更新于 …」会折成两行：会换行的标签在 `QHBoxLayout` 里
  只拿得到自己 sizeHint 那么宽，右边空着也不用。换成 `ElidedLabel` 并给它拉伸位。
- 「最近命中」空着时给了占位文案。
- 安装器给开始菜单和桌面快捷方式补上 `AppUserModelID`，和程序运行时声明的那个对齐。
  以前两者对不上，Windows 眼里任务栏上跑着的窗口和快捷方式是两个东西，右键「固定到任务栏」
  钉出来的是个点不开的空壳。值由 `build.py` 从 `core/version.py` 传进 ISCC，不在 `.iss` 里另写一份。

## 2026-08-27 — v2.0：重写为 Python，不再钉死在游戏目录

整个程序从 WinUI 3 / .NET 8 重写成 Python + PySide6，`CunSorter/` 整个删掉。
功能对齐 v1.3，`cun_config.json` 和 `cun_ocr_cache.json` 格式没变，老数据直接沿用。

### 换位置

以前程序必须解压到 `<CHUNITHM>in\cun\`，因为它靠「我在哪」去找同级的
`screenshots` 和配置文件。现在这条依赖断掉了：

| | 位置 |
|---|---|
| 程序 | `%LOCALAPPDATA%\Programs\今天你寸了吗\`（装到用户目录，不弹 UAC） |
| 配置 / 缓存 / 日志 | `%LOCALAPPDATA%\ChunithmCunSorter\` |
| 游戏目录 | 配置里的 `game_root`，安装向导选一次 |

安装向导多了一页「选择 CHUNITHM 游戏目录」，会先在几个常见位置猜一下，
用户确认或改掉即可，留空也行（第一次打开程序时还会再问，那时候能从
运行中的游戏进程反查出路径，更准）。选中的路径写成 `install.ini` 放在程序旁边，
程序第一次运行时读走填进配置。

老的 `bin\cun\` 部署仍然能用：exe 同级或上级目录有 `cun_config.json` 就是便携模式，
数据落那儿，原地升级不丢配置。

发行形式也跟着变了，从「解压到指定位置的 zip」改成 PyInstaller + Inno Setup 的安装包。

### 顺手修掉的三个真 bug

移植的时候拿 303 张真实结算截图和 v1.3 生产环境留下的缓存逐张对账，挖出三处：

1. **得分换算该截断，不是四舍五入。** 用顶栏的四个判定数按公式反算，在 150 张真实截图上
   截断值 149 次和画面上显示的得分完全相同，四舍五入有 54 次高出 1 分。v1.x 用的是
   `Math.Round`，所以联动那条路换算出来的分数系统性偏高 1，在评级边界上会改判
   （1,007,500 是 SSS，1,007,499 只是 SS+）。
   附带一个坑：改成截断之后必须用整数运算，浮点会把本该整除的值算成 `1009989.999…`，
   再截断又少 1。实测有 48 组判定数会踩到，其中 129 物量的全 JC 会被算成 1,009,999
   而不是满分。
2. **OCR 把「MISS : 1」读成「14」。** 数字紧贴顶栏右边界时，tesseract 把边缘噪点连了上去，
   7 张真实截图上都是这样。送 OCR 前补一圈留白就好了。但留白只能给顶栏那一行，
   得分那一行补了之后 psm 7 会整行读空，实测多出 115 处得分错误。
3. **带 UTF-8 BOM 的 `start.bat` 会被写坏。** BOM 粘在第一行内容前面，`@echo off` 匹配不上，
   自启动行被插到文件最前面，既排在 `@echo off` 之前又把 BOM 挤到了第二行。
   这个缺陷 v1.x 也有。

修完再对一遍：303 张截图上 ATTACK / MISS 与 v1.3 生产数据**零差异**，
得分只剩 40 处 ±1，全部是「旧值来自内存换算、新值来自 OCR」的预期差异。

### 别的

- 新增命令行入口 `cli.py`：`scan` / `watch` / `stats` / `config` / `game-root`，不用开界面。
- 新增首次运行向导，能从运行中的游戏进程反查出游戏目录。
- 新增配置项 `game_root`。
- 界面按 Apple HIG 深色模式重做：inset grouped 版式、拨动开关、活力橙强调色。
  Qt 没有拨动开关，自己画的。
- 依赖只有 PySide6 和 Pillow。进程、窗口、抓帧、注册表全走 ctypes 和 winreg，
  不引 pywin32 / psutil / mss。

## 2026-08-23 — 主题色换成活力橙

强调色以前跟着 **Windows 个性化设置**走（`AccentButtonStyle` / `AccentFillColorDefaultBrush`
读的都是系统强调色），几个自己写的 App 摆在一起长得一模一样，分不出谁是谁。
现在每个项目认领一种主题色（表在全局 `~/.claude/CLAUDE.md`），
「今天你寸了吗」拿到的是**活力橙 `#FF6B35`**——CHUNITHM 本来就是橙黄一系。

`CunSorter/App.xaml` 里覆盖掉这几样：

- `SystemAccentColor` 及 Light1/2/3、Dark1/2/3 六档（主色混白 / 混黑 14%、28%、42% 推出来的，别各写各的）
- `AccentFillColorDefaultBrush` / `Secondary` / `Tertiary`
- `TextOnAccentFillColorPrimaryBrush` 改成近黑 `#1F0A00`——白字压在这支橙上只有 2.84:1，黑字 7.41:1

Release x64 构建过，真开窗口截图核对：左侧导航选中条、输入框焦点线、判定规则那排开关都变橙了。
