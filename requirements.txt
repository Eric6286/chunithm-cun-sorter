# -*- coding: utf-8 -*-
# 上面那行不是摆设：老版本 pip 读 requirements.txt 时按系统区域编码解码，
# 中文 Windows 上是 GBK，会被下面的中文注释噎住（UnicodeDecodeError）。
# pip 认 PEP 263 风格的编码声明，写在前两行之内即可。
#
# 「寸录」的依赖。就这两个 + 打包和测试工具。
#
# 刻意不引 pywin32 / psutil / mss：进程、窗口、抓帧、注册表全走标准库的
# ctypes 和 winreg。少一个二进制依赖就少一类「装上跑不起来」。

PySide6>=6.6.0
Pillow>=10.0.0

# --- 只有打包和测试才要 ---
pyinstaller>=6.0.0
pytest>=8.0.0
