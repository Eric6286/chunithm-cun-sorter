# 「今天你寸了吗」的依赖。就这两个 + 打包工具。
#
# 刻意不引 pywin32 / psutil / mss：进程、窗口、抓帧、注册表全走标准库的
# ctypes 和 winreg。少一个二进制依赖就少一类「装上跑不起来」。

PySide6>=6.6.0
Pillow>=10.0.0

# --- 只有打包和测试才要 ---
pyinstaller>=6.0.0
pytest>=8.0.0
